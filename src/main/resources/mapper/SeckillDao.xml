<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.waynechu.mmall.mapper.RoleMapper">
    <select id="getRolesByRoleIds" resultType="cn.waynechu.mmall.entity.Role">
        select id,name,name_zh,info,create_time,update_time from t_role where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>